version: "3.9"

services:
  db:
    image: postgres:16
    container_name: asmo_pg
    environment:
      POSTGRES_DB: asmo
      POSTGRES_USER: asmo_user
      POSTGRES_PASSWORD: asmo_pass
    ports:
      - "5432:5432"